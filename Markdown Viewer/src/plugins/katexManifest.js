const katexMetadata = Object.freeze({
  name: 'KaTeX Math Renderer',
  version: '1.1.0',
  description: 'Renders local LaTeX math only when a document contains configured math syntax.',
  author: 'Markdown Editor',
  defaultEnabled: true
});

export { katexMetadata };
